# Dedicated crawl4ai venv (heavy, machine-specific)
crawl4ai/.venv/
# Crawl outputs & cache
crawl4ai/outputs/
crawl4ai/.crawl4ai/
# update check cache (local, per-machine)
.update_check.json

# Python
__pycache__/
*.pyc
.DS_Store

# Secrets — never commit real credentials (templates *.example are allowed)
.env
.env.*
*.env
.secret
.secrets
*.secret
*.secrets
!.env.example
!*.env.example

# page-monitor snapshots (local state)
page_monitor/snapshots/

# Dedicated keyword_cluster heavy venv (machine-specific)
keyword_cluster/.venv/
keyword_cluster/outputs/*.png
keyword_cluster/.env

# Claude Code local state / worktrees
.claude/

# whitening backgrounds — large matrices stay local

# embedding cache (local, per-machine)
keyword_cluster/cache/

# d4s response cache (local, per-machine)
d4s/cache/
